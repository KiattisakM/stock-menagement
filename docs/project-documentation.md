# โปรเจค: ระบบจัดการสต็อกวัสดุก่อสระและเงินเดือนพนักงาน

> 📌 **สำหรับ Claude Code**: เอกสารนี้ออกแบบมาให้ Claude Code สามารถอ่านและทำความเข้าใจเพื่อช่วยพัฒนาโปรเจคได้อย่างมีประสิทธิภาพ

---

## 🤖 คำแนะนำสำหรับ Claude Code

### วิธีใช้เอกสารนี้กับ Claude Code

1. **อ่านเอกสารทั้งหมด**: Claude Code จะอ่านและเข้าใจโครงสร้างโปรเจคโดยรวม
2. **ทำงานทีละ Phase**: แนะนำให้ทำตาม Development Roadmap (Phase 1-5)
3. **ใช้ตัวอย่างโค้ด**: ในเอกสารมีตัวอย่างโค้ดให้เป็นแนวทาง
4. **ตรวจสอบ Database Schema**: ก่อนเขียนโค้ด ให้ดูโครงสร้างฐานข้อมูลก่อน

### คำสั่งที่แนะนำให้ใช้กับ Claude Code

```bash
# เริ่มโปรเจคใหม่
claude code "สร้างโปรเจค Next.js 16 ตามเอกสาร project-documentation.md โดยเริ่มจาก Phase 1"

# สร้าง Database Schema
claude code "สร้าง Prisma schema ตามโครงสร้างในเอกสาร ส่วนของ Database Schema"

# สร้าง Feature เฉพาะ
claude code "สร้างหน้าบันทึกสต็อกเข้า ตามที่ระบุในเอกสาร section 2.1"

# Setup shadcn/ui
claude code "ติดตั้งและ configure shadcn/ui components ที่จำเป็นทั้งหมด"

# สร้าง PDF Template
claude code "สร้าง SalarySlipPDF component ด้วย react-pdf ตามตัวอย่างในเอกสาร"
```

### สิ่งที่ Claude Code ควรทำอัตโนมัติ

- ✅ Setup โปรเจค Next.js 16 พร้อม TypeScript
- ✅ ติดตั้ง dependencies ทั้งหมดตาม Technology Stack
- ✅ สร้าง Prisma schema และ run migrations
- ✅ Setup shadcn/ui และติดตั้ง components ที่จำเป็น
- ✅ สร้างโครงสร้างโฟลเดอร์ตาม App Router pattern
- ✅ Implement authentication system
- ✅ สร้าง API routes สำหรับแต่ละ feature
- ✅ สร้าง UI components ด้วย shadcn/ui
- ✅ Implement form validation ด้วย React Hook Form + Zod
- ✅ สร้าง PDF templates ด้วย @react-pdf/renderer

### หมายเหตุสำคัญสำหรับ Claude Code

1. **ใช้ App Router**: ทุก page ต้องอยู่ใน `/app` directory
2. **Server Components by default**: ใช้ `'use client'` เมื่อจำเป็นเท่านั้น
3. **Tailwind v4 syntax**: ใช้ CSS-first configuration
4. **shadcn/ui components**: Copy components จาก CLI ก่อนใช้งาน
5. **Type Safety**: ใช้ TypeScript อย่างเคร่งครัด (no `any` type)
6. **Prisma Best Practices**: ใช้ transactions สำหรับ operations ที่สำคัญ

---

## 📋 ภาพรวมโปรเจค

ระบบ Web Application สำหรับจัดการสต็อกวัสดุก่อสราง (หิน ดิน ทราย) และระบบคำนวณเงินเดือนพนักงานขับรถบรรทุก

---

## 📁 โครงสร้างโปรเจคที่แนะนำ

```
stock-management/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   └── layout.tsx
│   ├── (dashboard)/
│   │   ├── dashboard/
│   │   │   └── page.tsx
│   │   ├── stock/
│   │   │   ├── in/
│   │   │   │   └── page.tsx
│   │   │   ├── out/
│   │   │   │   └── page.tsx
│   │   │   └── history/
│   │   │       └── page.tsx
│   │   ├── employees/
│   │   │   ├── page.tsx
│   │   │   ├── [id]/
│   │   │   ├── trips/
│   │   │   └── salary/
│   │   ├── reports/
│   │   │   └── page.tsx
│   │   └── layout.tsx
│   ├── api/
│   │   ├── stock/
│   │   │   ├── in/route.ts
│   │   │   ├── out/route.ts
│   │   │   └── materials/route.ts
│   │   ├── employees/
│   │   │   ├── route.ts
│   │   │   ├── trips/route.ts
│   │   │   └── advances/route.ts
│   │   └── auth/
│   │       └── [...nextauth]/route.ts
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── ui/                    # shadcn/ui components
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── table.tsx
│   │   └── ...
│   ├── forms/
│   │   ├── stock-in-form.tsx
│   │   ├── stock-out-form.tsx
│   │   └── trip-form.tsx
│   ├── tables/
│   │   ├── stock-history-table.tsx
│   │   └── employee-table.tsx
│   └── pdf/
│       ├── salary-slip.tsx
│       ├── stock-report.tsx
│       └── monthly-report.tsx
├── lib/
│   ├── prisma.ts              # Prisma Client instance
│   ├── auth.ts                # Authentication utilities
│   ├── utils.ts               # Utility functions
│   └── validations/
│       ├── stock.ts           # Zod schemas for stock
│       └── employee.ts        # Zod schemas for employee
├── prisma/
│   ├── schema.prisma
│   └── migrations/
├── public/
├── .env
├── .env.example
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

---

## 🎯 คำสั่ง Prompt ตัวอย่างสำหรับ Claude Code

### Phase 1: Project Setup

**Prompt 1: สร้างโปรเจคเริ่มต้น**
```
สร้างโปรเจค Next.js 16 ใหม่ชื่อ "stock-management" พร้อม:
- TypeScript
- Tailwind CSS v4
- App Router
- src/ directory

จากนั้นติดตั้ง dependencies ทั้งหมดที่ระบุใน Technology Stack
```

**Prompt 2: Setup Prisma**
```
Setup Prisma สำหรับ PostgreSQL และสร้าง schema.prisma ตามโครงสร้างฐานข้อมูลทั้งหมดที่ระบุในเอกสาร
รวมถึง relations และ indexes ที่จำเป็น
```

**Prompt 3: Setup shadcn/ui**
```
Initialize shadcn/ui และติดตั้ง components ทั้งหมดที่จำเป็น:
button, input, select, table, form, card, badge, alert, tabs, sheet, dialog, calendar, date-picker

Configure theme สำหรับโปรเจคนี้
```

---

### Phase 2: Stock Management

**Prompt 4: สร้างระบบจัดการวัสดุ**
```
สร้างหน้า CRUD สำหรับจัดการวัสดุ (/app/(dashboard)/stock/materials) พร้อม:
- ตารางแสดงรายการวัสดุทั้งหมด
- ฟอร์มเพิ่ม/แก้ไขวัสดุ (ใช้ shadcn/ui Dialog)
- Form validation ด้วย React Hook Form + Zod
- API routes สำหรับ CRUD operations
- ใช้ Prisma สำหรับ database operations
```

**Prompt 5: สร้างหน้าบันทึกสต็อกเข้า**
```
สร้างหน้าบันทึกสต็อกเข้า (/app/(dashboard)/stock/in) ตาม Use Case 1 ในเอกสาร พร้อม:
- ฟอร์มบันทึกสต็อกเข้า (ใช้ shadcn/ui Form)
- Dropdown เลือกวัสดุ (ดึงจาก database)
- Date/Time picker (default วันนี้)
- Form validation
- API route สำหรับบันทึกข้อมูล
- อัพเดท current_stock อัตโนมัติ
- แสดง toast notification เมื่อสำเร็จ
```

**Prompt 6: สร้างหน้าบันทึกสต็อกออก**
```
สร้างหน้าบันทึกสต็อกออก (/app/(dashboard)/stock/out) คล้าย stock/in แต่:
- เช็คว่า quantity <= current_stock ก่อนบันทึก
- อัพเดท current_stock ด้วยการลบ
- มีฟิลด์เพิ่มเติม: ชื่อลูกค้า, ชื่อโครงการ
```

---

### Phase 3: Employee & Payroll

**Prompt 7: สร้างระบบจัดการพนักงาน**
```
สร้างหน้าจัดการพนักงาน (/app/(dashboard)/employees) พร้อม:
- ตารางแสดงพนักงานทั้งหมด (ใช้ shadcn/ui DataTable)
- ฟอร์ม CRUD พนักงาน
- กรองตามสถานะ (active/inactive)
- แสดงข้อมูล: ชื่อ, ทะเบียนรถ, เงินเดือนฐาน, อัตราค่าเที่ยว
```

**Prompt 8: สร้างหน้าบันทึกการวิ่งงาน**
```
สร้างหน้าบันทึกการวิ่งงาน (/app/(dashboard)/employees/trips) ตาม Use Case 2 พร้อม:
- Dropdown เลือกพนักงาน
- Auto-fill ค่าเที่ยวจาก rate_per_trip
- สามารถบันทึกหลายเที่ยวในครั้งเดียว
- แสดงประวัติการวิ่งงานของพนักงานที่เลือก
```

**Prompt 9: สร้างระบบคำนวณเงินเดือน**
```
สร้างหน้าคำนวณเงินเดือน (/app/(dashboard)/employees/salary) ตาม Use Case 3 พร้อม:
- เลือกเดือน-ปี
- ปุ่ม "คำนวณเงินเดือน" ที่:
  * ดึงข้อมูล trips และ advances
  * คำนวณตามสูตร: net_salary = (base_salary + total_trip_income) - total_advances
  * สร้างหรืออัพเดท salary_summaries
- ตารางแสดงผลสรุปเงินเดือนทุกคน
- ปุ่มพิมพ์สลิปแต่ละคน
```

---

### Phase 4: PDF & Reports

**Prompt 10: สร้าง PDF Template สำหรับสลิปเงินเดือน**
```
สร้าง Component SalarySlipPDF ด้วย @react-pdf/renderer ที่:
- รับ props: employee, month, year, salaryData
- แสดงข้อมูล: ชื่อพนักงาน, เดือน/ปี, จำนวนเที่ยว, รายได้, เงินเบิก, เงินสุทธิ
- จัด layout ให้สวยงาม พร้อม logo (optional)
- รองรับฟอนต์ไทย
- สร้าง PDFDownloadLink component
```

**Prompt 11: สร้างระบบ Export Excel**
```
สร้าง utility function สำหรับ export Excel ด้วย ExcelJS:
- exportStockHistory(data, filename)
- exportSalaryReport(data, filename)
- มี styling และ formatting
- รองรับข้อมูลภาษาไทย
```

---

### Phase 5: Dashboard & Alerts

**Prompt 12: สร้าง Dashboard**
```
สร้างหน้า Dashboard (/app/(dashboard)/dashboard) พร้อม:
- Card แสดงสรุปสต็อกแต่ละประเภท (ใช้ shadcn/ui Card)
- Alert แจ้งเตือนสต็อกต่ำ (query จาก materials WHERE current_stock <= min_stock_alert)
- Card แสดงสรุปเงินเดือนเดือนนี้
- กราฟแสดงการเคลื่อนไหวสต็อก (ใช้ Recharts)
- สรุปจำนวนเที่ยววิ่งงานวันนี้
```

**Prompt 13: Setup Line Notify & Alerts**
```
Setup Line Notify integration และระบบแจ้งเตือน:
- สร้าง lib/line-notify.ts สำหรับส่งข้อความ LINE
- สร้าง API route /api/cron/stock-alert สำหรับตรวจสอบสต็อกต่ำ
- Setup node-cron ให้ส่งแจ้งเตือนทุกเช้า 08:00 น.
- สร้าง StockAlerts component สำหรับแสดงบน Dashboard
- ใช้ TanStack Query polling ทุก 30 วินาที
- เพิ่ม LINE_NOTIFY_TOKEN ใน .env.example
```

---

## 🎯 Features MVP (Minimum Viable Product)

### 1. ระบบจัดการสต็อก
- ✅ บันทึกรับสินค้า (สต็อกเข้า)
  - หิน/ดิน/ทราย
  - จำนวน, หน่วย (ตัน/คิว)
  - วันที่-เวลา
  - ผู้ส่ง
  - ราคา
- ✅ บันทึกจ่ายสินค้า (สต็อกออก)
  - ส่งให้ลูกค้า/โครงการไหน
  - จำนวน
  - วันที่-เวลา
- ✅ ดูยอดคงเหลือแยกตามประเภทวัสดุ
- ✅ ประวัติการเข้า-ออกเพื่อตรวจสอบย้อนหลัง

### 2. ระบบจัดการเงินเดือนพนักงาน
- ✅ ข้อมูลพนักงาน
  - ชื่อ
  - เลขทะเบียนรถ
  - เงินเดือนฐาน (ถ้ามี)
- ✅ บันทึกการวิ่งงาน
  - วันที่
  - เที่ยวที่
  - เส้นทาง
  - ค่าเที่ยว (Fixed rate)
- ✅ บันทึกเบิกเงินล่วงหน้า
  - วันที่เบิก
  - จำนวนเงิน
- ✅ คำนวณเงินเดือน
  - รายได้รวม - เงินเบิกล่วงหน้า = เงินคงเหลือจ่าย

### 3. Features เพิ่มเติม
- 🔔 แจ้งเตือนเมื่อสต็อกใกล้หมด
  - แสดงบน Dashboard (Real-time)
  - Line Notify (ทุกเช้า 08:00 น.)
- 🖨️ พิมพ์รายงาน (PDF)
- 📧 ส่งข้อมูลให้ลูกค้า (Excel)

---

## 🏗️ สถาปัตยกรรมระบบ

### ข้อกำหนดเบื้องต้น
- **ประเภท**: Web Application (เข้าผ่านเบราว์เซอร์)
- **ผู้ใช้งาน**: เจ้าของธุรกิจคนเดียว (ในระยะแรก)
- **การคำนวณค่าวิ่งงาน**: Fixed rate (เที่ยวละเท่าไหร่)

### Technology Stack

#### Frontend & Backend (Full-stack)
- **Next.js 16** - Full-stack React Framework (App Router)
- **React 19** - UI Library
- **TypeScript** - Type Safety
- **Tailwind CSS v4** - Modern CSS Framework พร้อม `@tailwindcss/vite`
- **shadcn/ui** - High-quality React Components (built on Radix UI)

#### Database & ORM
- **PostgreSQL** - ระบบฐานข้อมูล (Production-ready)
- **Prisma** - Modern ORM สำหรับ TypeScript

#### UI Components & Styling
- **Radix UI** - Accessible UI Primitives (จาก shadcn/ui)
- **Lucide React** - Icon Library
- **Recharts** - แสดงกราฟและ Analytics

#### Document & Export
- **@react-pdf/renderer** - สร้าง PDF ด้วย React Components
- **ExcelJS** - Export/Import ไฟล์ Excel
- **xlsx** - อ่านไฟล์ Excel (ถ้าต้องการ Import)

#### Notifications & Scheduling
- **node-cron** - จัดการ Scheduled Tasks (แจ้งเตือนอัตโนมัติ)
- **Line Notify API** - ส่งการแจ้งเตือนผ่าน LINE

#### Additional Features
- **Zod** - Schema Validation
- **React Hook Form** - Form Management
- **TanStack Query** (React Query) - Data Fetching & Caching

---

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง: materials (ข้อมูลวัสดุ)
```sql
CREATE TABLE materials (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,              -- ชื่อวัสดุ: หิน, ดิน, ทราย
  unit VARCHAR(20) NOT NULL,               -- หน่วย: ตัน, คิว
  current_stock DECIMAL(10,2) DEFAULT 0,   -- สต็อกปัจจุบัน
  min_stock_alert DECIMAL(10,2),           -- จุดแจ้งเตือนสต็อกต่ำ
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**ตัวอย่างข้อมูล:**
| id | name | unit | current_stock | min_stock_alert |
|----|------|------|---------------|-----------------|
| 1  | หินฝุ่น | ตัน | 50.00 | 10.00 |
| 2  | ทรายหยาบ | คิว | 30.00 | 5.00 |

---

### ตาราง: stock_in (บันทึกสต็อกเข้า)
```sql
CREATE TABLE stock_in (
  id INT PRIMARY KEY AUTO_INCREMENT,
  material_id INT NOT NULL,
  quantity DECIMAL(10,2) NOT NULL,         -- จำนวนที่รับเข้า
  unit_price DECIMAL(10,2),                -- ราคาต่อหน่วย
  supplier VARCHAR(200),                   -- ผู้จำหน่าย/ผู้ส่ง
  note TEXT,                               -- หมายเหตุ
  transaction_date DATETIME NOT NULL,      -- วันที่-เวลาที่รับเข้า
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (material_id) REFERENCES materials(id)
);
```

---

### ตาราง: stock_out (บันทึกสต็อกออก)
```sql
CREATE TABLE stock_out (
  id INT PRIMARY KEY AUTO_INCREMENT,
  material_id INT NOT NULL,
  quantity DECIMAL(10,2) NOT NULL,         -- จำนวนที่จ่ายออก
  customer_name VARCHAR(200),              -- ชื่อลูกค้า
  project_name VARCHAR(200),               -- ชื่อโครงการ
  note TEXT,                               -- หมายเหตุ
  transaction_date DATETIME NOT NULL,      -- วันที่-เวลาที่จ่ายออก
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (material_id) REFERENCES materials(id)
);
```

---

### ตาราง: employees (ข้อมูลพนักงาน)
```sql
CREATE TABLE employees (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,              -- ชื่อพนักงาน
  truck_license VARCHAR(50),               -- ทะเบียนรถ
  base_salary DECIMAL(10,2) DEFAULT 0,     -- เงินเดือนฐาน
  rate_per_trip DECIMAL(10,2) NOT NULL,    -- อัตราค่าเที่ยว (Fixed rate)
  status ENUM('active', 'inactive') DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**ตัวอย่างข้อมูล:**
| id | name | truck_license | base_salary | rate_per_trip | status |
|----|------|---------------|-------------|---------------|--------|
| 1  | สมชาย | กข-1234 | 0.00 | 500.00 | active |
| 2  | สมหญิง | กข-5678 | 5000.00 | 400.00 | active |

---

### ตาราง: trips (บันทึกการวิ่งงาน)
```sql
CREATE TABLE trips (
  id INT PRIMARY KEY AUTO_INCREMENT,
  employee_id INT NOT NULL,
  trip_date DATE NOT NULL,                 -- วันที่วิ่งงาน
  route VARCHAR(200),                      -- เส้นทาง
  material_id INT,                         -- วัสดุที่บรรทุก
  quantity DECIMAL(10,2),                  -- จำนวนที่บรรทุก
  rate DECIMAL(10,2) NOT NULL,             -- ค่าเที่ยวนี้
  note TEXT,                               -- หมายเหตุ
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (employee_id) REFERENCES employees(id),
  FOREIGN KEY (material_id) REFERENCES materials(id)
);
```

---

### ตาราง: advances (บันทึกเบิกเงินล่วงหน้า)
```sql
CREATE TABLE advances (
  id INT PRIMARY KEY AUTO_INCREMENT,
  employee_id INT NOT NULL,
  amount DECIMAL(10,2) NOT NULL,           -- จำนวนเงินที่เบิก
  advance_date DATE NOT NULL,              -- วันที่เบิก
  note TEXT,                               -- หมายเหตุ
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (employee_id) REFERENCES employees(id)
);
```

---

### ตาราง: salary_summaries (สรุปเงินเดือนรายเดือน)
```sql
CREATE TABLE salary_summaries (
  id INT PRIMARY KEY AUTO_INCREMENT,
  employee_id INT NOT NULL,
  month INT NOT NULL,                      -- เดือน (1-12)
  year INT NOT NULL,                       -- ปี
  total_trips INT DEFAULT 0,               -- จำนวนเที่ยวทั้งหมด
  total_trip_income DECIMAL(10,2) DEFAULT 0,  -- รายได้จากการวิ่งงาน
  total_advances DECIMAL(10,2) DEFAULT 0,  -- เงินเบิกล่วงหน้าทั้งหมด
  base_salary DECIMAL(10,2) DEFAULT 0,     -- เงินเดือนฐาน
  net_salary DECIMAL(10,2) DEFAULT 0,      -- เงินเดือนสุทธิ (รายได้รวม - เงินเบิก)
  is_paid BOOLEAN DEFAULT FALSE,           -- จ่ายแล้วหรือยัง
  paid_date DATE,                          -- วันที่จ่าย
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (employee_id) REFERENCES employees(id),
  UNIQUE KEY unique_employee_month (employee_id, month, year)
);
```

**สูตรการคำนวณ:**
```
net_salary = (base_salary + total_trip_income) - total_advances
```

---

## 📱 หน้าจอหลักของระบบ

### 1. Dashboard (หน้าแรก) 📊
**จุดประสงค์:** ภาพรวมของธุรกิจในมุมมองเดียว

**องค์ประกอบ:**
- 📦 สรุปยอดสต็อกทั้งหมด (แยกตามประเภทวัสดุ)
- ⚠️ การแจ้งเตือนสต็อกใกล้หมด
- 💰 สรุปค่าใช้จ่ายพนักงานเดือนปัจจุบัน
- 📈 กราฟแสดงการเคลื่อนไหวสต็อก
- 🚚 สรุปจำนวนเที่ยววิ่งงานวันนี้/สัปดาห์นี้

---

### 2. จัดการสต็อก 📦

#### 2.1 บันทึกสต็อกเข้า
**ฟิลด์ที่ต้องกรอก:**
- เลือกประเภทวัสดุ (dropdown)
- จำนวน
- หน่วย (auto-fill จากวัสดุที่เลือก)
- ราคาต่อหน่วย
- ผู้จำหน่าย/ผู้ส่ง
- วันที่-เวลา (default: ปัจจุบัน)
- หมายเหตุ (optional)

**การทำงาน:**
```
เมื่อบันทึก → current_stock += quantity
```

---

#### 2.2 บันทึกสต็อกออก
**ฟิลด์ที่ต้องกรอก:**
- เลือกประเภทวัสดุ (dropdown)
- จำนวน
- ชื่อลูกค้า
- ชื่อโครงการ
- วันที่-เวลา (default: ปัจจุบัน)
- หมายเหตุ (optional)

**การทำงาน:**
```
ตรวจสอบ: quantity <= current_stock
เมื่อบันทึก → current_stock -= quantity
```

---

#### 2.3 ประวัติการเข้า-ออก
**ฟีเจอร์:**
- ตารางแสดงรายการทั้งหมด (เรียงตามวันที่ล่าสุด)
- กรองตามประเภท (เข้า/ออก)
- กรองตามวัสดุ
- กรองตามช่วงวันที่
- ค้นหา
- Export เป็น Excel/PDF

---

#### 2.4 รายงานสต็อกคงเหลือ
**ฟีเจอร์:**
- แสดงยอดคงเหลือแต่ละประเภท
- แสดงมูลค่ารวม
- กราฟแสดงสัดส่วนสต็อก
- Export เป็น PDF

---

### 3. จัดการพนักงาน 👷

#### 3.1 รายชื่อพนักงาน
**ฟีเจอร์:**
- ตารางแสดงรายชื่อพนักงานทั้งหมด
- เพิ่ม/แก้ไข/ลบข้อมูลพนักงาน
- กรองตามสถานะ (active/inactive)

**ข้อมูลในตาราง:**
- ชื่อ
- ทะเบียนรถ
- เงินเดือนฐาน
- อัตราค่าเที่ยว
- สถานะ
- Actions (แก้ไข/ลบ)

---

#### 3.2 บันทึกการวิ่งงานประจำวัน
**ฟิลด์ที่ต้องกรอก:**
- เลือกพนักงาน (dropdown)
- วันที่วิ่งงาน (default: วันนี้)
- เส้นทาง
- วัสดุที่บรรทุก (optional)
- จำนวนที่บรรทุก (optional)
- ค่าเที่ยว (auto-fill จาก rate_per_trip ของพนักงาน)
- หมายเหตุ (optional)

**ฟีเจอร์เพิ่มเติม:**
- บันทึกหลายเที่ยวในครั้งเดียว
- ดูประวัติการวิ่งงานของพนักงานแต่ละคน

---

#### 3.3 บันทึกเบิกเงินล่วงหน้า
**ฟิลด์ที่ต้องกรอก:**
- เลือกพนักงาน (dropdown)
- จำนวนเงิน
- วันที่เบิก (default: วันนี้)
- หมายเหตุ (optional)

**ฟีเจอร์เพิ่มเติม:**
- ดูยอดเบิกสะสมของพนักงานแต่ละคน
- ประวัติการเบิกเงิน

---

#### 3.4 คำนวณเงินเดือนรายเดือน
**ฟีเจอร์:**
- เลือกเดือน-ปี
- แสดงตารางสรุปเงินเดือนพนักงานทั้งหมด

**คอลัมน์ในตาราง:**
- ชื่อพนักงาน
- เงินเดือนฐาน
- จำนวนเที่ยว
- รายได้จากวิ่งงาน
- เงินเบิกล่วงหน้า
- **เงินเดือนสุทธิ** (highlight)
- สถานะจ่าย
- Actions (ทำเครื่องหมายจ่ายแล้ว, พิมพ์สลิป)

**ปุ่มดำเนินการ:**
- คำนวณเงินเดือน (สร้าง salary_summaries)
- Export รายงาน
- ส่งสลิปเงินเดือนทาง Email

---

### 4. รายงาน 📄

#### 4.1 รายงานสต็อก
**ประเภทรายงาน:**
- รายงานสต็อกคงเหลือ
- รายงานการเคลื่อนไหวสต็อก (ตามช่วงเวลา)
- รายงานมูลค่าสต็อก

**Format:**
- PDF (พิมพ์/ส่งให้ลูกค้า)
- Excel (วิเคราะห์ข้อมูล)

---

#### 4.2 รายงานเงินเดือนพนักงาน
**ประเภทรายงาน:**
- สลิปเงินเดือนรายบุคคล
- รายงานสรุปเงินเดือนทั้งหมด (ตามเดือน)
- รายงานการวิ่งงานแต่ละคน

**Format:**
- PDF (สลิปเงินเดือน)
- Excel (สรุปรายเดือน)

---

#### 4.3 รายงานรายรับ-รายจ่าย
**ประเภทรายงาน:**
- รายได้จากการขายวัสดุ
- ค่าใช้จ่ายพนักงาน
- กำไร-ขาดทุน

**Format:**
- PDF
- Excel

---

## 🔔 ระบบแจ้งเตือน

### 1. แจ้งเตือนสต็อกใกล้หมด
**เงื่อนไข:**
```sql
SELECT * FROM materials 
WHERE current_stock <= min_stock_alert
```

**ช่องทางแจ้งเตือน:**
- ✅ **แสดงบนหน้า Dashboard (Real-time)**
  - ใช้ TanStack Query เพื่อ polling ทุก 30 วินาที
  - แสดงเป็น Alert component จาก shadcn/ui
  - มี badge สีแดงแสดงจำนวนรายการที่ต่ำ
- ✅ **Line Notify (ทุกเช้า 08:00 น.)**
  - ส่งสรุปสต็อกที่ต่ำกว่าจุดแจ้งเตือน
  - ใช้ Node-cron สำหรับ schedule

**ตัวอย่างข้อความ Line Notify:**
```
⚠️ แจ้งเตือนสต็อกต่ำ

📦 หินฝุ่น
สต็อกคงเหลือ: 8 ตัน
จุดแจ้งเตือน: 10 ตัน

📦 ทรายหยาบ
สต็อกคงเหลือ: 3 คิว
จุดแจ้งเตือน: 5 คิว

กรุณาสั่งซื้อเพิ่ม
```

---

### Implementation Guide

#### Setup Line Notify

**ขั้นตอนการตั้งค่า:**

1. **ขอ Line Notify Token:**
   - ไปที่ https://notify-bot.line.me/
   - Login ด้วย LINE account
   - ไปที่ "My Page" → "Generate token"
   - เลือก 1-on-1 chat กับ LINE Notify หรือ group ที่ต้องการ
   - Copy token ที่ได้

2. **เพิ่มใน Environment Variables:**
   ```env
   # .env
   LINE_NOTIFY_TOKEN=your_line_notify_token_here
   ```

3. **สร้าง Line Notify Utility:**
   ```typescript
   // lib/line-notify.ts
   export async function sendLineNotify(message: string) {
     const token = process.env.LINE_NOTIFY_TOKEN;
     
     if (!token) {
       console.error('LINE_NOTIFY_TOKEN not found');
       return false;
     }
     
     const response = await fetch('https://notify-api.line.me/api/notify', {
       method: 'POST',
       headers: {
         'Content-Type': 'application/x-www-form-urlencoded',
         'Authorization': `Bearer ${token}`
       },
       body: new URLSearchParams({ message })
     });
     
     return response.ok;
   }
   ```

4. **สร้าง API Route สำหรับแจ้งเตือนสต็อก:**
   ```typescript
   // app/api/cron/stock-alert/route.ts
   import { prisma } from '@/lib/prisma';
   import { sendLineNotify } from '@/lib/line-notify';
   
   export async function GET() {
     // Query low stock items
     const lowStockItems = await prisma.materials.findMany({
       where: {
         current_stock: {
           lte: prisma.raw('min_stock_alert')
         }
       }
     });
     
     if (lowStockItems.length > 0) {
       let message = '⚠️ แจ้งเตือนสต็อกต่ำ\n\n';
       
       lowStockItems.forEach(item => {
         message += `📦 ${item.name}\n`;
         message += `สต็อกคงเหลือ: ${item.current_stock} ${item.unit}\n`;
         message += `จุดแจ้งเตือน: ${item.min_stock_alert} ${item.unit}\n\n`;
       });
       
       message += 'กรุณาสั่งซื้อเพิ่ม';
       
       await sendLineNotify(message);
     }
     
     return Response.json({ 
       success: true, 
       alertCount: lowStockItems.length 
     });
   }
   ```

5. **Setup Cron Job:**
   ```typescript
   // app/api/cron/schedule/route.ts
   import cron from 'node-cron';
   
   // รันทุกเช้า 08:00 น.
   cron.schedule('0 8 * * *', async () => {
     try {
       await fetch(`${process.env.NEXT_PUBLIC_URL}/api/cron/stock-alert`);
       console.log('Stock alert sent at', new Date());
     } catch (error) {
       console.error('Failed to send stock alert:', error);
     }
   });
   
   export async function GET() {
     return Response.json({ status: 'Cron jobs configured' });
   }
   ```

---

#### Dashboard Real-time Alerts

**สร้าง API Route สำหรับ Dashboard:**
```typescript
// app/api/stock/low-stock/route.ts
import { prisma } from '@/lib/prisma';

export async function GET() {
  const lowStockItems = await prisma.materials.findMany({
    where: {
      current_stock: {
        lte: prisma.raw('min_stock_alert')
      }
    },
    select: {
      id: true,
      name: true,
      current_stock: true,
      min_stock_alert: true,
      unit: true
    }
  });
  
  return Response.json(lowStockItems);
}
```

**Dashboard Alert Component:**
```typescript
// components/dashboard/stock-alerts.tsx
'use client';

import { useQuery } from '@tanstack/react-query';
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';
import { Badge } from '@/components/ui/badge';
import { AlertTriangle } from 'lucide-react';

export function StockAlerts() {
  const { data: lowStockItems } = useQuery({
    queryKey: ['low-stock'],
    queryFn: async () => {
      const res = await fetch('/api/stock/low-stock');
      return res.json();
    },
    refetchInterval: 30000, // Refresh ทุก 30 วินาที
  });

  if (!lowStockItems?.length) return null;

  return (
    <Alert variant="destructive" className="mb-4">
      <AlertTriangle className="h-4 w-4" />
      <AlertTitle className="flex items-center gap-2">
        สต็อกใกล้หมด 
        <Badge variant="destructive">{lowStockItems.length} รายการ</Badge>
      </AlertTitle>
      <AlertDescription>
        <ul className="mt-2 space-y-2">
          {lowStockItems.map((item) => (
            <li key={item.id} className="text-sm">
              <strong>{item.name}</strong>: เหลือ {item.current_stock} {item.unit}
              {' '}(ต่ำกว่า {item.min_stock_alert} {item.unit})
            </li>
          ))}
        </ul>
      </AlertDescription>
    </Alert>
  );
}
```

**เพิ่มใน Dashboard Page:**
```typescript
// app/(dashboard)/dashboard/page.tsx
import { StockAlerts } from '@/components/dashboard/stock-alerts';

export default function DashboardPage() {
  return (
    <div className="space-y-4">
      <h1>Dashboard</h1>
      
      {/* Real-time Stock Alerts */}
      <StockAlerts />
      
      {/* Dashboard content */}
      {/* ... */}
    </div>
  );
}
```

---

### ทดสอบระบบแจ้งเตือน

**1. ทดสอบ Line Notify:**
```bash
# ส่งข้อความทดสอบ
curl -X POST http://localhost:3000/api/cron/stock-alert
```

**2. ทดสอบ Dashboard Alert:**
- เปิดเบราว์เซอร์ไปที่ `/dashboard`
- ลด `current_stock` ของวัสดุใดๆ ให้ต่ำกว่า `min_stock_alert`
- Alert จะแสดงขึ้นอัตโนมัติภายใน 30 วินาที

---

---

## 🛠️ เทคนิคและ Best Practices

### Next.js 16 App Router
- ใช้ **App Router** (ไม่ใช้ Pages Router)
- **Server Components** เป็น default
- Client Components เมื่อจำเป็น (`'use client'`)
- **Server Actions** สำหรับ Form Submission และ Mutations
- **Route Handlers** สำหรับ API Endpoints (`/app/api`)

**โครงสร้างโฟลเดอร์:**
```
/app
  /dashboard          # หน้า Dashboard
  /stock
    /in               # บันทึกสต็อกเข้า
    /out              # บันทึกสต็อกออก
  /employees          # จัดการพนักงาน
  /reports            # รายงานต่างๆ
  /api                # API Routes
    /stock
    /employees
  layout.tsx          # Root Layout
  page.tsx            # Home page
```

---

### Tailwind CSS v4 Features
- **New CSS-first Configuration** - ใช้ `@import` แทน `tailwind.config.js`
- **Improved Performance** - Fast refresh และ build time
- **Modern CSS Variables** - Better theming support
- **Native Cascade Layers** - Better specificity control

**ตัวอย่าง CSS Configuration:**
```css
/* app/globals.css */
@import "tailwindcss";

@theme {
  --color-primary: #3b82f6;
  --color-secondary: #10b981;
  --radius-md: 0.5rem;
}
```

---

### shadcn/ui Integration
- **Component-based Architecture** - Copy components ที่ต้องการเข้าโปรเจค
- **Customizable** - แก้ไข source code ได้เต็มที่
- **Accessible** - Built on Radix UI (ARIA compliant)
- **Dark Mode** - Support out of the box

**Components ที่จะใช้:**
- `Button`, `Input`, `Select`, `Dialog`, `Table`
- `Form` (with React Hook Form integration)
- `Card`, `Badge`, `Alert`
- `Tabs`, `Sheet`, `Popover`
- `Calendar`, `DatePicker`
- `DataTable` (for displaying lists)

**ติดตั้ง shadcn/ui:**
```bash
npx shadcn@latest init
npx shadcn@latest add button input select table form
```

---

### React-PDF สำหรับสร้างรายงาน

**ตัวอย่างการสร้าง PDF ด้วย @react-pdf/renderer:**

```typescript
import { Document, Page, Text, View, StyleSheet, PDFDownloadLink } from '@react-pdf/renderer';

// สร้าง styles
const styles = StyleSheet.create({
  page: {
    padding: 30,
    fontFamily: 'Helvetica',
  },
  header: {
    fontSize: 24,
    marginBottom: 20,
    textAlign: 'center',
  },
  table: {
    display: 'flex',
    width: 'auto',
    borderStyle: 'solid',
    borderWidth: 1,
  },
});

// Component สำหรับ PDF
const SalarySlipPDF = ({ employee, month, year, data }) => (
  <Document>
    <Page size="A4" style={styles.page}>
      <Text style={styles.header}>สลิปเงินเดือน</Text>
      <View>
        <Text>พนักงาน: {employee.name}</Text>
        <Text>เดือน: {month}/{year}</Text>
        <Text>จำนวนเที่ยว: {data.totalTrips}</Text>
        <Text>รายได้: {data.totalIncome} บาท</Text>
        <Text>เงินเบิก: {data.totalAdvances} บาท</Text>
        <Text>เงินสุทธิ: {data.netSalary} บาท</Text>
      </View>
    </Page>
  </Document>
);

// ใช้งานใน Component
export default function SalaryReport() {
  return (
    <PDFDownloadLink 
      document={<SalarySlipPDF {...data} />} 
      fileName="salary-slip.pdf"
    >
      {({ loading }) => loading ? 'กำลังสร้าง PDF...' : 'ดาวน์โหลดสลิป'}
    </PDFDownloadLink>
  );
}
```

**PDF Templates ที่ต้องสร้าง:**
1. สลิปเงินเดือนพนักงาน
2. รายงานสต็อกคงเหลือ
3. รายงานการเคลื่อนไหวสต็อก
4. รายงานสรุปเงินเดือนรายเดือน

---

## 🚀 แผนการพัฒนา (Development Roadmap)

### Phase 1: Setup & Core Features (สัปดาห์ที่ 1-2)
- [ ] **Setup Project**
  ```bash
  npx create-next-app@latest stock-management --typescript --tailwind --app
  cd stock-management
  
  # ติดตั้ง shadcn/ui
  npx shadcn@latest init
  
  # ติดตั้ง dependencies
  npm install @prisma/client @react-pdf/renderer
  npm install -D prisma
  npm install react-hook-form zod @hookform/resolvers
  npm install @tanstack/react-query
  npm install exceljs
  npm install node-cron
  npm install -D @types/node-cron
  
  # Setup Prisma
  npx prisma init
  ```
- [ ] **Configure Tailwind v4**
  - Setup `@import "tailwindcss"` ใน globals.css
  - Configure theme variables
  - Setup dark mode
- [ ] **Setup shadcn/ui Components**
  ```bash
  npx shadcn@latest add button input select table form card badge alert
  npx shadcn@latest add tabs sheet dialog calendar date-picker
  ```
- [ ] **Database Setup**
  - สร้างโครงสร้างตารางใน `schema.prisma`
  - Run migrations: `npx prisma migrate dev`
  - Setup Prisma Client
- [ ] **Authentication System**
  - Simple login (email + password)
  - Session management
  - Protected routes
- [ ] **Dashboard Layout**
  - Sidebar navigation
  - Header with user info
  - Responsive design

### Phase 2: Stock Management (สัปดาห์ที่ 3-4)
- [ ] CRUD วัสดุ (เพิ่ม/แก้ไข/ลบ)
- [ ] บันทึกสต็อกเข้า
- [ ] บันทึกสต็อกออก
- [ ] แสดงสต็อกคงเหลือ
- [ ] ประวัติการเข้า-ออก

### Phase 3: Employee & Payroll (สัปดาห์ที่ 5-6)
- [ ] CRUD พนักงาน
- [ ] บันทึกการวิ่งงาน
- [ ] บันทึกเบิกเงินล่วงหน้า
- [ ] คำนวณเงินเดือนรายเดือน
- [ ] แสดงสรุปเงินเดือน

### Phase 4: Reports & Alerts (สัปดาห์ที่ 7-8)
- [ ] **PDF Generation with @react-pdf/renderer**
  - สร้าง PDF Templates:
    - SalarySlipPDF Component
    - StockReportPDF Component
    - MonthlyReportPDF Component
  - Implement PDF Download
  - Implement PDF Preview
  - ใช้ฟอนต์ไทย (TH Sarabun New)
- [ ] **Excel Export with ExcelJS**
  - Export สต็อกเป็น Excel
  - Export รายงานเงินเดือนเป็น Excel
  - Formatting และ Styling
- [ ] **Alert System**
  - Real-time stock alerts บน Dashboard (TanStack Query polling)
  - Line Notify integration
  - Cron jobs สำหรับแจ้งเตือนประจำวัน (08:00 น.)
  - ทดสอบการส่ง Line Notify

### Phase 5: Testing & Deployment (สัปดาห์ที่ 9-10)
- [ ] Testing ทุก Feature
- [ ] แก้ Bug
- [ ] Deployment (Vercel/Railway/DigitalOcean)
- [ ] User Acceptance Testing
- [ ] Launch 🎉

---

## 📝 ตัวอย่าง Use Cases

### Use Case 1: รับสต็อกหินเข้า
```
1. เจ้าของเข้าหน้า "บันทึกสต็อกเข้า"
2. เลือกวัสดุ: "หินฝุ่น"
3. กรอกจำนวน: 20 ตัน
4. กรอกราคา: 15,000 บาท (750 บาท/ตัน)
5. กรอกผู้ส่ง: "บริษัท ABC"
6. กดบันทึก
→ ระบบอัพเดท current_stock จาก 30 → 50 ตัน
→ บันทึกข้อมูลใน stock_in
→ แสดงข้อความ "บันทึกสำเร็จ"
```

---

### Use Case 2: บันทึกการวิ่งงาน
```
1. เจ้าของเข้าหน้า "บันทึกการวิ่งงาน"
2. เลือกพนักงาน: "สมชาย"
3. กรอกวันที่: "วันนี้"
4. กรอกเส้นทาง: "กรุงเทพ - ชลบุรี"
5. เลือกวัสดุ: "ทรายหยาบ"
6. ค่าเที่ยว: 500 บาท (auto-fill)
7. กดบันทึก
→ ระบบบันทึกข้อมูลใน trips
→ อัพเดทจำนวนเที่ยวของสมชายในเดือนนี้
→ แสดงข้อความ "บันทึกสำเร็จ"
```

---

### Use Case 3: คำนวณเงินเดือนประจำเดือน
```
1. เจ้าของเข้าหน้า "คำนวณเงินเดือน"
2. เลือกเดือน: "มกราคม 2026"
3. กดปุ่ม "คำนวณ"
→ ระบบดึงข้อมูล:
  - trips ทั้งหมดของพนักงานในเดือนมกราคม
  - advances ทั้งหมดของพนักงานในเดือนมกราคม
→ คำนวณ:
  - total_trips = 25 เที่ยว
  - total_trip_income = 25 × 500 = 12,500 บาท
  - total_advances = 3,000 บาท
  - net_salary = (0 + 12,500) - 3,000 = 9,500 บาท
→ สร้าง salary_summaries
→ แสดงตารางสรุป
4. กดปุ่ม "พิมพ์สลิปเงินเดือน"
→ ระบบสร้างไฟล์ PDF
→ ดาวน์โหลดอัตโนมัติ
```

---

## 🔐 Security Considerations

### 1. Authentication & Authorization
- ระบบ Login/Logout
- Session Management
- Password Hashing (bcrypt)
- Role-based Access (เตรียมไว้สำหรับอนาคต)

### 2. Data Validation
- Validate ข้อมูลทุกครั้งก่อนบันทึก
- Prevent SQL Injection (ใช้ Prepared Statements)
- Sanitize Input

### 3. Backup
- Auto-backup ฐานข้อมูลทุกวัน
- Export ข้อมูลเป็น Excel/CSV ได้

---

## 💡 Future Enhancements (อนาคต)

### Phase 6+: Advanced Features
- 📱 Mobile App (React Native)
- 👥 Multi-user Support (พนักงานหลายคน login ได้)
- 📸 Upload รูปใบเสร็จ/ใบส่งของ
- 🔍 Advanced Analytics & Business Intelligence
- 📊 Dashboard แบบ Real-time
- 🤖 AI Prediction สำหรับการสั่งซื้อสต็อก
- 🔗 Integration กับระบบบัญชี
- 💳 ระบบจ่ายเงินออนไลน์
- 📲 Line OA สำหรับแจ้งเตือน

---

## 📚 เอกสารอ้างอิง

### Official Documentation
- [Next.js 16 Documentation](https://nextjs.org/docs)
- [React 19 Documentation](https://react.dev)
- [Tailwind CSS v4 Documentation](https://tailwindcss.com/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com)
- [Prisma Documentation](https://www.prisma.io/docs)

### Libraries & Tools
- [@react-pdf/renderer](https://react-pdf.org/) - PDF Generation
- [Radix UI](https://www.radix-ui.com/) - UI Primitives (shadcn/ui ใช้)
- [TanStack Query](https://tanstack.com/query/latest) - Data Fetching
- [React Hook Form](https://react-hook-form.com/) - Form Management
- [Zod](https://zod.dev/) - Schema Validation
- [ExcelJS](https://github.com/exceljs/exceljs) - Excel Processing
- [node-cron](https://github.com/node-cron/node-cron) - Task Scheduling
- [LINE Notify API](https://notify-bot.line.me/doc/en/) - LINE Notifications

### Tutorials & Guides
- [Next.js App Router Tutorial](https://nextjs.org/learn)
- [Prisma Getting Started](https://www.prisma.io/docs/getting-started)
- [shadcn/ui Installation](https://ui.shadcn.com/docs/installation/next)
- [React PDF Examples](https://react-pdf.org/repl)
- [Tailwind CSS v4 Migration Guide](https://tailwindcss.com/docs/v4-beta)

---

## 📞 Support & Contact

หากมีคำถามหรือต้องการความช่วยเหลือเพิ่มเติม สามารถติดต่อได้ที่:
- GitHub Issues
- Email: [your-email@example.com]

---

## ✅ Checklist สำหรับเริ่มต้น

### Environment Setup
- [ ] ติดตั้ง Node.js 18+ (Next.js 16 requires Node 18.18+)
- [ ] ติดตั้ง npm/yarn/pnpm (แนะนำ pnpm)
- [ ] ติดตั้ง PostgreSQL 14+
- [ ] ติดตั้ง Git
- [ ] Setup IDE (VS Code แนะนำ)
  - [ ] ติดตั้ง Extensions:
    - ESLint
    - Prettier
    - Tailwind CSS IntelliSense
    - Prisma

### Project Initialization
- [ ] สร้างโปรเจค Next.js 16
  ```bash
  npx create-next-app@latest
  ```
- [ ] ติดตั้ง shadcn/ui
  ```bash
  npx shadcn@latest init
  ```
- [ ] Setup Prisma
  ```bash
  npx prisma init
  ```
- [ ] Configure Tailwind v4
- [ ] ติดตั้ง dependencies ทั้งหมด

### Before Development
- [ ] อ่านเอกสารนี้ให้ครบ
- [ ] ศึกษา Next.js 16 App Router
- [ ] ทำความเข้าใจ shadcn/ui component system
- [ ] ศึกษา Prisma schema design
- [ ] วางแผน Database schema

### Ready to Start
- [ ] Setup database connection
- [ ] Run first migration
- [ ] Test development server
- [ ] เริ่ม Phase 1 Development 🚀

---

**Last Updated:** 5 มกราคม 2026  
**Version:** 1.2 (Claude Code Ready)  
**Author:** Claude AI Assistant

**Tech Stack:**
- Next.js 16 (App Router)
- React 19
- Tailwind CSS v4
- shadcn/ui
- @react-pdf/renderer
- Prisma + PostgreSQL

**Optimized for:** Claude Code CLI  
**Compatibility:** ✅ Ready for autonomous development
